#include <cstdint> // C Standard Integer library
                   // it an successor of c programms stdint

int main() {
    std::uint8_t x;
    std::uint16_t y;
    std::uint32_t z;
    std::uint64_t zz;
    std::int32_t sz;
}
